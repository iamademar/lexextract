[pytest]
testpaths = tests
asyncio_mode = auto
addopts = --cache-clear --tb=short -v
# Cache clearing helps with import issues
# Short tracebacks for cleaner output  
# Verbose output for better test visibility 